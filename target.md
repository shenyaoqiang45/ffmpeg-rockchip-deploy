# 目标

数据定义：A = YUV 输入数据，B = MJPEG 数据，C = YUV 输出数据。

1. 图像分辨率：1600×1200
2. FFmpeg 硬件编码 YUV 格式数据 A，提取第一帧并转换为 MJPEG 数据 B，并进行耗时统计
3. FFmpeg 硬件编码 MJPEG 格式数据 B，提取第一帧并转换为 YUV 格式数据 C

## 评价标准

1. 压缩比：YUV 格式数据 A 与 YUV 格式数据 C 的压缩比为 3:1
2. 指标：对 YUV 格式数据 A 与 YUV 格式数据 C 进行量化计算 **PSNR**（峰值信噪比）和 **SSIM**（结构相似性）
